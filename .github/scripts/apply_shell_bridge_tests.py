from pathlib import Path

shell_path = Path('app/src/main/java/com/gree1d/reappzuku/core/ShellManager.java')
bridge_path = Path('app/src/main/java/com/gree1d/reappzuku/core/ShizukuBridge.java')
test_path = Path('app/src/androidTest/java/com/gree1d/reappzuku/core/ShellManagerStateInstrumentationTest.java')

bridge_path.write_text('''package com.gree1d.reappzuku.core;\n\nimport android.content.ServiceConnection;\n\nimport rikka.shizuku.Shizuku;\n\n/** Package-private seam around the static Shizuku API for deterministic state-machine tests. */\ninterface ShizukuBridge {\n    boolean pingBinder();\n    int checkSelfPermission();\n    void requestPermission(int requestCode);\n    void addRequestPermissionResultListener(Shizuku.OnRequestPermissionResultListener listener);\n    void removeRequestPermissionResultListener(Shizuku.OnRequestPermissionResultListener listener);\n    void addBinderReceivedListenerSticky(Shizuku.OnBinderReceivedListener listener);\n    void removeBinderReceivedListener(Shizuku.OnBinderReceivedListener listener);\n    void addBinderDeadListener(Shizuku.OnBinderDeadListener listener);\n    void removeBinderDeadListener(Shizuku.OnBinderDeadListener listener);\n    void bindUserService(Shizuku.UserServiceArgs args, ServiceConnection connection);\n    void unbindUserService(Shizuku.UserServiceArgs args, ServiceConnection connection, boolean remove);\n}\n\nfinal class RealShizukuBridge implements ShizukuBridge {\n    @Override public boolean pingBinder() { return Shizuku.pingBinder(); }\n    @Override public int checkSelfPermission() { return Shizuku.checkSelfPermission(); }\n    @Override public void requestPermission(int requestCode) { Shizuku.requestPermission(requestCode); }\n    @Override public void addRequestPermissionResultListener(Shizuku.OnRequestPermissionResultListener listener) {\n        Shizuku.addRequestPermissionResultListener(listener);\n    }\n    @Override public void removeRequestPermissionResultListener(Shizuku.OnRequestPermissionResultListener listener) {\n        Shizuku.removeRequestPermissionResultListener(listener);\n    }\n    @Override public void addBinderReceivedListenerSticky(Shizuku.OnBinderReceivedListener listener) {\n        Shizuku.addBinderReceivedListenerSticky(listener);\n    }\n    @Override public void removeBinderReceivedListener(Shizuku.OnBinderReceivedListener listener) {\n        Shizuku.removeBinderReceivedListener(listener);\n    }\n    @Override public void addBinderDeadListener(Shizuku.OnBinderDeadListener listener) {\n        Shizuku.addBinderDeadListener(listener);\n    }\n    @Override public void removeBinderDeadListener(Shizuku.OnBinderDeadListener listener) {\n        Shizuku.removeBinderDeadListener(listener);\n    }\n    @Override public void bindUserService(Shizuku.UserServiceArgs args, ServiceConnection connection) {\n        Shizuku.bindUserService(args, connection);\n    }\n    @Override public void unbindUserService(Shizuku.UserServiceArgs args, ServiceConnection connection, boolean remove) {\n        Shizuku.unbindUserService(args, connection, remove);\n    }\n}\n''')

shell = shell_path.read_text()
field_anchor = '    private final ExecutorService executor;\n\n    private volatile Boolean hasRoot = null;'
field_replace = '    private final ExecutorService executor;\n    private final ShizukuBridge shizuku;\n\n    private volatile Boolean hasRoot = null;'
if field_anchor not in shell:
    raise SystemExit('ShellManager field anchor not found')
shell = shell.replace(field_anchor, field_replace, 1)

ctor_anchor = '''    public ShellManager(Context context, Handler handler, ExecutorService executor) {\n        this.context = context.getApplicationContext();\n        this.handler = handler;\n        this.executor = executor;\n\n        // Keep an application-lifetime listener. Activity listeners can be removed\n        // while the system permission dialog is in the foreground, so binding the\n        // UserService must not depend on an Activity still being started.\n        Shizuku.addRequestPermissionResultListener(internalShizukuPermissionListener);\n        Shizuku.addBinderReceivedListenerSticky(internalBinderReceivedListener);\n        Shizuku.addBinderDeadListener(internalBinderDeadListener);\n    }'''
ctor_replace = '''    public ShellManager(Context context, Handler handler, ExecutorService executor) {\n        this(context, handler, executor, new RealShizukuBridge());\n    }\n\n    ShellManager(Context context, Handler handler, ExecutorService executor, ShizukuBridge shizuku) {\n        if (shizuku == null) throw new IllegalArgumentException("shizuku == null");\n        this.context = context.getApplicationContext();\n        this.handler = handler;\n        this.executor = executor;\n        this.shizuku = shizuku;\n\n        // Keep an application-lifetime listener. Activity listeners can be removed\n        // while the system permission dialog is in the foreground, so binding the\n        // UserService must not depend on an Activity still being started.\n        shizuku.addRequestPermissionResultListener(internalShizukuPermissionListener);\n        shizuku.addBinderReceivedListenerSticky(internalBinderReceivedListener);\n        shizuku.addBinderDeadListener(internalBinderDeadListener);\n    }'''
if ctor_anchor not in shell:
    raise SystemExit('ShellManager constructor anchor not found')
shell = shell.replace(ctor_anchor, ctor_replace, 1)

replacements = {\n    'Shizuku.pingBinder()': 'shizuku.pingBinder()',\n    'Shizuku.checkSelfPermission()': 'shizuku.checkSelfPermission()',\n    'Shizuku.bindUserService(buildUserServiceArgs(), userServiceConnection)': 'shizuku.bindUserService(buildUserServiceArgs(), userServiceConnection)',\n    'Shizuku.unbindUserService(buildUserServiceArgs(), userServiceConnection, true)': 'shizuku.unbindUserService(buildUserServiceArgs(), userServiceConnection, true)',\n    'Shizuku.addRequestPermissionResultListener(listener)': 'shizuku.addRequestPermissionResultListener(listener)',\n    'Shizuku.removeRequestPermissionResultListener(listener)': 'shizuku.removeRequestPermissionResultListener(listener)',\n    'Shizuku.addBinderReceivedListenerSticky(shizukuBinderReceivedListener)': 'shizuku.addBinderReceivedListenerSticky(shizukuBinderReceivedListener)',\n    'Shizuku.addBinderDeadListener(shizukuBinderDeadListener)': 'shizuku.addBinderDeadListener(shizukuBinderDeadListener)',\n    'Shizuku.removeBinderReceivedListener(shizukuBinderReceivedListener)': 'shizuku.removeBinderReceivedListener(shizukuBinderReceivedListener)',\n    'Shizuku.removeBinderDeadListener(shizukuBinderDeadListener)': 'shizuku.removeBinderDeadListener(shizukuBinderDeadListener)',\n    'Shizuku.requestPermission(SHIZUKU_PERMISSION_REQUEST_CODE)': 'shizuku.requestPermission(SHIZUKU_PERMISSION_REQUEST_CODE)',\n}
for old, new in replacements.items():
    if old not in shell:
        raise SystemExit(f'ShellManager Shizuku call anchor not found: {old}')
    shell = shell.replace(old, new)

# No direct static operational Shizuku calls should remain; type references are allowed.
for forbidden in ('Shizuku.pingBinder()', 'Shizuku.checkSelfPermission()', 'Shizuku.requestPermission(',\n                  'Shizuku.bindUserService(', 'Shizuku.unbindUserService(',\n                  'Shizuku.addRequestPermissionResultListener(', 'Shizuku.removeRequestPermissionResultListener(',\n                  'Shizuku.addBinderReceivedListenerSticky(', 'Shizuku.removeBinderReceivedListener(',\n                  'Shizuku.addBinderDeadListener(', 'Shizuku.removeBinderDeadListener('):
    if forbidden in shell:
        raise SystemExit(f'unmigrated Shizuku call remains: {forbidden}')
shell_path.write_text(shell)

test_path.write_text('''package com.gree1d.reappzuku.core;\n\nimport static org.junit.Assert.assertEquals;\n\nimport android.content.ComponentName;\nimport android.content.Context;\nimport android.content.ServiceConnection;\nimport android.content.pm.PackageManager;\nimport android.os.Handler;\nimport android.os.Looper;\n\nimport androidx.test.platform.app.InstrumentationRegistry;\n\nimport com.gree1d.reappzuku.core.shell.IShellCallback;\nimport com.gree1d.reappzuku.core.shell.IShellService;\nimport com.gree1d.reappzuku.core.shell.ProcessMemoryInfo;\nimport com.gree1d.reappzuku.core.shell.ShellExecResult;\n\nimport org.junit.After;\nimport org.junit.Before;\nimport org.junit.Test;\n\nimport java.util.ArrayList;\nimport java.util.List;\nimport java.util.concurrent.ExecutorService;\nimport java.util.concurrent.Executors;\nimport java.util.concurrent.atomic.AtomicInteger;\n\nimport rikka.shizuku.Shizuku;\n\npublic class ShellManagerStateInstrumentationTest {\n    private Context context;\n    private ExecutorService executor;\n\n    @Before\n    public void setUp() {\n        context = InstrumentationRegistry.getInstrumentation().getTargetContext();\n        executor = Executors.newSingleThreadExecutor();\n    }\n\n    @After\n    public void tearDown() {\n        executor.shutdownNow();\n    }\n\n    @Test\n    public void binderLateDenyThenGrantReachesReady() {\n        FakeShizukuBridge bridge = new FakeShizukuBridge(context);\n        ShellManager manager = new ShellManager(\n                context, new Handler(Looper.getMainLooper()), executor, bridge);\n\n        assertEquals(ShellBackendState.SHIZUKU_UNAVAILABLE, manager.getBackendState());\n\n        bridge.setBinderAvailable(true);\n        bridge.fireBinderReceived();\n        assertEquals(ShellBackendState.SHIZUKU_PERMISSION_REQUIRED, manager.getBackendState());\n\n        manager.checkShellPermissions();\n        assertEquals(1, bridge.permissionRequestCount);\n        assertEquals(ShellBackendState.SHIZUKU_PERMISSION_PENDING, manager.getBackendState());\n\n        bridge.firePermissionResult(PackageManager.PERMISSION_DENIED);\n        assertEquals(ShellBackendState.SHIZUKU_PERMISSION_REQUIRED, manager.getBackendState());\n\n        manager.checkShellPermissions();\n        assertEquals(2, bridge.permissionRequestCount);\n        bridge.firePermissionResult(PackageManager.PERMISSION_GRANTED);\n        assertEquals(ShellBackendState.SHIZUKU_BINDING, manager.getBackendState());\n\n        bridge.connectUserService();\n        assertEquals(ShellBackendState.SHIZUKU_READY, manager.getBackendState());\n    }\n\n    @Test\n    public void permissionGrantStillBindsAfterActivityListenerIsRemoved() {\n        FakeShizukuBridge bridge = new FakeShizukuBridge(context);\n        bridge.setBinderAvailable(true);\n        ShellManager manager = new ShellManager(\n                context, new Handler(Looper.getMainLooper()), executor, bridge);\n        AtomicInteger activityCallbacks = new AtomicInteger();\n        Shizuku.OnRequestPermissionResultListener activityListener =\n                (requestCode, grantResult) -> activityCallbacks.incrementAndGet();\n        manager.setShizukuPermissionListener(activityListener);\n\n        manager.checkShellPermissions();\n        manager.removeShizukuPermissionListener(activityListener);\n        bridge.firePermissionResult(PackageManager.PERMISSION_GRANTED);\n\n        assertEquals(0, activityCallbacks.get());\n        assertEquals(ShellBackendState.SHIZUKU_BINDING, manager.getBackendState());\n        bridge.connectUserService();\n        assertEquals(ShellBackendState.SHIZUKU_READY, manager.getBackendState());\n    }\n\n    @Test\n    public void binderDeathThenReturnRebindsUserService() {\n        FakeShizukuBridge bridge = new FakeShizukuBridge(context);\n        bridge.setBinderAvailable(true);\n        bridge.permission = PackageManager.PERMISSION_GRANTED;\n        ShellManager manager = new ShellManager(\n                context, new Handler(Looper.getMainLooper()), executor, bridge);\n        assertEquals(ShellBackendState.SHIZUKU_BINDING, manager.getBackendState());\n        bridge.connectUserService();\n        assertEquals(ShellBackendState.SHIZUKU_READY, manager.getBackendState());\n        assertEquals(1, bridge.bindCount);\n\n        bridge.setBinderAvailable(false);\n        bridge.fireBinderDead();\n        assertEquals(ShellBackendState.SHIZUKU_LOST, manager.getBackendState());\n\n        bridge.setBinderAvailable(true);\n        bridge.fireBinderReceived();\n        assertEquals(ShellBackendState.SHIZUKU_BINDING, manager.getBackendState());\n        assertEquals(2, bridge.bindCount);\n        bridge.connectUserService();\n        assertEquals(ShellBackendState.SHIZUKU_READY, manager.getBackendState());\n    }\n\n    private static final class FakeShizukuBridge implements ShizukuBridge {\n        private final Context context;\n        private final List<Shizuku.OnRequestPermissionResultListener> permissionListeners = new ArrayList<>();\n        private final List<Shizuku.OnBinderReceivedListener> receivedListeners = new ArrayList<>();\n        private final List<Shizuku.OnBinderDeadListener> deadListeners = new ArrayList<>();\n        private boolean binderAvailable;\n        private int permission = PackageManager.PERMISSION_DENIED;\n        private int permissionRequestCount;\n        private int bindCount;\n        private ServiceConnection pendingConnection;\n\n        FakeShizukuBridge(Context context) {\n            this.context = context;\n        }\n\n        void setBinderAvailable(boolean available) {\n            binderAvailable = available;\n        }\n\n        void fireBinderReceived() {\n            for (Shizuku.OnBinderReceivedListener listener : new ArrayList<>(receivedListeners)) {\n                listener.onBinderReceived();\n            }\n        }\n\n        void fireBinderDead() {\n            pendingConnection = null;\n            for (Shizuku.OnBinderDeadListener listener : new ArrayList<>(deadListeners)) {\n                listener.onBinderDead();\n            }\n        }\n\n        void firePermissionResult(int result) {\n            permission = result;\n            for (Shizuku.OnRequestPermissionResultListener listener : new ArrayList<>(permissionListeners)) {\n                listener.onRequestPermissionResult(0, result);\n            }\n        }\n\n        void connectUserService() {\n            if (pendingConnection == null) throw new AssertionError("no pending UserService bind");\n            IShellService.Stub service = new IShellService.Stub() {\n                @Override public ShellExecResult execute(String command) {\n                    return new ShellExecResult(true, 0, "");\n                }\n                @Override public void executeWithCallback(String command, IShellCallback callback) { }\n                @Override public ProcessMemoryInfo[] getProcessMemoryInfo(int[] pids) {\n                    return new ProcessMemoryInfo[0];\n                }\n                @Override public void destroy() { }\n            };\n            ServiceConnection connection = pendingConnection;\n            pendingConnection = null;\n            connection.onServiceConnected(\n                    new ComponentName(context.getPackageName(), "FakeShellService"),\n                    service.asBinder());\n        }\n\n        @Override public boolean pingBinder() { return binderAvailable; }\n        @Override public int checkSelfPermission() { return permission; }\n        @Override public void requestPermission(int requestCode) { permissionRequestCount++; }\n        @Override public void addRequestPermissionResultListener(Shizuku.OnRequestPermissionResultListener listener) {\n            permissionListeners.add(listener);\n        }\n        @Override public void removeRequestPermissionResultListener(Shizuku.OnRequestPermissionResultListener listener) {\n            permissionListeners.remove(listener);\n        }\n        @Override public void addBinderReceivedListenerSticky(Shizuku.OnBinderReceivedListener listener) {\n            receivedListeners.add(listener);\n            if (binderAvailable) listener.onBinderReceived();\n        }\n        @Override public void removeBinderReceivedListener(Shizuku.OnBinderReceivedListener listener) {\n            receivedListeners.remove(listener);\n        }\n        @Override public void addBinderDeadListener(Shizuku.OnBinderDeadListener listener) {\n            deadListeners.add(listener);\n        }\n        @Override public void removeBinderDeadListener(Shizuku.OnBinderDeadListener listener) {\n            deadListeners.remove(listener);\n        }\n        @Override public void bindUserService(Shizuku.UserServiceArgs args, ServiceConnection connection) {\n            bindCount++;\n            pendingConnection = connection;\n        }\n        @Override public void unbindUserService(Shizuku.UserServiceArgs args, ServiceConnection connection, boolean remove) {\n            if (pendingConnection == connection) pendingConnection = null;\n        }\n    }\n}\n''')
