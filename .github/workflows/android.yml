name: Legacy Android CI (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired. Use the source-authoritative validation and signed-release workflows.'
